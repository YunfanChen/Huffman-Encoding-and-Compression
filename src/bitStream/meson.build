bitStream = library('bitStream', dependencies : [input_dep, output_dep, bitStream_dep])
inc = include_directories('.')

bitStream_dep = declare_dependency(include_directories : inc,
  link_with : bitStream)